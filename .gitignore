# Claude Code local config (machine-specific permissions / paths / plugins)
.claude/

# Build intermediate (base64 blob emitted by build-bat.ps1)
alleaves_b64.txt

# Quarantined output of a failed build-bat.ps1 self-verify
*.bat.corrupt

# Runtime working state created on target terminals
AlleavesAuto/
downloads/
logs/
*.log

# Installer payloads / binaries pulled at runtime
*.exe
*.msi
*.zip

# OS cruft
Thumbs.db
desktop.ini
.DS_Store
